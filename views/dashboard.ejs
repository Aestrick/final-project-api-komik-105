<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - KomikAPI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .custom-scrollbar::-webkit-scrollbar { width: 8px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: #1f2937; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #4b5563; border-radius: 4px; }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: #6b7280; }
    </style>
</head>
<body class="bg-gray-900 text-gray-100 font-sans pb-20">

    <nav class="bg-gray-800 border-b border-gray-700 p-4 sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500">
                KomikAPI 🚀
            </h1>
            <div class="flex items-center gap-4">
                <span class="text-gray-400 text-sm hidden md:inline">Halo, <b class="text-white"><%= user.username %></b></span>
                <a href="/login" class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white text-xs rounded-lg transition">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto mt-10 p-4">
        
        <div class="bg-gray-800 rounded-2xl p-8 border border-gray-700 shadow-2xl max-w-4xl mx-auto mb-10">
            <h2 class="text-2xl font-bold mb-2">Selamat Datang, Developer! 👋</h2>
            <p class="text-gray-400 mb-8">Ini adalah kunci rahasia untuk mengakses semua data komik premium kami.</p>

            <div class="bg-gray-900 p-6 rounded-xl border border-dashed border-gray-600">
                <div class="flex justify-between items-end mb-2">
                    <label class="text-xs text-gray-500 uppercase tracking-wider font-bold">Your Personal API Key</label>
                    <form action="/regenerate-key" method="POST">
                        <input type="hidden" name="email" value="<%= user.email %>">
                        <button type="submit" class="text-xs flex items-center gap-1 text-yellow-500 hover:text-yellow-400 transition font-bold" onclick="return confirm('Yakin mau ganti API Key?')">
                            🔄 Ganti Key Baru
                        </button>
                    </form>
                </div>
                
                <div class="flex items-center gap-2">
                    <code class="flex-1 bg-black/50 p-4 rounded-lg text-green-400 font-mono text-sm break-all" id="apiKeyDisplay">
                        <%= user.api_key %>
                    </code>
                    <button onclick="copyKey()" class="bg-gray-700 hover:bg-gray-600 text-white p-4 rounded-lg transition border border-gray-600" title="Copy Key">
                        📋
                    </button>
                </div>
            </div>

            <div class="mt-8">
                <h3 class="font-bold text-lg mb-4">Cara Pakai:</h3>
                <div class="bg-black/30 p-4 rounded-lg text-sm text-gray-300 font-mono overflow-x-auto">
                    GET http://localhost:3000/api/v1/public/komik?api_key=<span class="text-green-400">PASTE_KEY_DISINI</span>
                </div>
            </div>
        </div>

        <div class="max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8">
            
            <div class="bg-gray-800 rounded-2xl p-6 border border-gray-700 shadow-xl">
                <div class="flex items-center gap-2 mb-6">
                    <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-white font-bold">1</div>
                    <h3 class="text-xl font-bold text-white">Input Komik Baru</h3>
                </div>
                
                <form action="/tambah-komik-web" method="POST" class="space-y-4">
                    <input type="hidden" name="email" value="<%= user.email %>">
                    <div>
                        <label class="block text-gray-400 text-xs font-bold uppercase mb-2">Judul Komik</label>
                        <input type="text" name="judul" required placeholder="Contoh: Naruto Shipuden" class="w-full bg-gray-900 border border-gray-600 rounded-lg p-3 text-white focus:border-blue-500 outline-none transition">
                    </div>
                    <div>
                        <label class="block text-gray-400 text-xs font-bold uppercase mb-2">Penulis</label>
                        <input type="text" name="penulis" required placeholder="Contoh: Masashi Kishimoto" class="w-full bg-gray-900 border border-gray-600 rounded-lg p-3 text-white focus:border-blue-500 outline-none transition">
                    </div>
                    <div>
                        <label class="block text-gray-400 text-xs font-bold uppercase mb-2">Deskripsi Singkat</label>
                        <textarea name="deskripsi" placeholder="Cerita tentang ninja..." class="w-full bg-gray-900 border border-gray-600 rounded-lg p-3 text-white focus:border-blue-500 outline-none h-24 transition"></textarea>
                    </div>
                    <button type="submit" class="w-full bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white font-bold py-3 rounded-lg transition transform hover:scale-[1.02]">
                        + Simpan ke Database
                    </button>
                </form>
            </div>

            <div class="bg-gray-800 rounded-2xl p-6 border border-gray-700 shadow-xl overflow-hidden flex flex-col h-[500px]">
                
                <div class="flex justify-between items-center mb-4">
                    <div class="flex items-center gap-2">
                        <div class="w-8 h-8 rounded-full bg-purple-500 flex items-center justify-center text-white font-bold">2</div>
                        <h3 class="text-xl font-bold text-white">Database API</h3>
                    </div>
                    <span class="text-xs text-gray-400 bg-gray-900 px-2 py-1 rounded border border-gray-700">
                        Total: <%= user.list_komik ? user.list_komik.length : 0 %> Data
                    </span>
                </div>

                <div class="mb-4 relative">
                    <input type="text" id="liveSearchInput" onkeyup="filterKomik()" placeholder="⚡ Ketik buat nyari (Live)..." 
                        class="w-full bg-gray-900 border border-gray-600 rounded-lg pl-4 pr-10 py-2 text-sm text-white focus:border-purple-500 outline-none transition">
                    <span class="absolute right-3 top-2 text-gray-500">🔍</span>
                </div>
                
                <div class="overflow-y-auto flex-1 pr-2 space-y-3 custom-scrollbar" id="komikListContainer">
                    <% if(user.list_komik && user.list_komik.length > 0) { %>
                        <% user.list_komik.forEach(komik => { %>
                            <div class="komik-item bg-gray-900 p-4 rounded-lg border-l-4 border-purple-500 hover:bg-gray-900/80 transition group flex justify-between items-center">
                                <div>
                                    <h4 class="judul-komik font-bold text-white text-lg"><%= komik.judul %></h4>
                                    <p class="text-xs text-gray-400">Penulis: <span class="text-gray-300"><%= komik.penulis %></span></p>
                                </div>
                                <form action="/delete-komik/<%= komik.id %>" method="POST" onsubmit="return confirm('Hapus?')">
                                    <button type="submit" class="text-red-500 hover:text-red-400 p-2 text-xl" title="Hapus Data">🗑️</button>
                                </form>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="h-full flex flex-col items-center justify-center text-gray-500 opacity-50">
                            <p class="text-center">Database masih kosong.<br>Input data di samping ya!</p>
                        </div>
                    <% } %>
                    
                    <div id="noResultMsg" class="hidden text-center text-gray-500 mt-10">
                        <p>Yah, komik yang dicari gak ada bro 😢</p>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <script>
        function copyKey() {
            const key = document.getElementById('apiKeyDisplay').innerText;
            navigator.clipboard.writeText(key.trim());
            alert('API Key berhasil disalin!');
        }

        // --- SCRIPT LIVE SEARCH ---
        function filterKomik() {
            // 1. Ambil apa yang diketik user & jadiin HURUF KECIL semua (Biar gak case sensitive)
            const input = document.getElementById('liveSearchInput').value.toLowerCase();
            
            // 2. Ambil semua kartu komik
            const cards = document.getElementsByClassName('komik-item');
            let foundCount = 0;

            // 3. Loop cek satu-satu
            for (let i = 0; i < cards.length; i++) {
                // Ambil judul dari kartu
                const title = cards[i].getElementsByClassName('judul-komik')[0].innerText.toLowerCase();
                
                // Cek: Apakah judul mengandung huruf yang diketik?
                if (title.indexOf(input) > -1) {
                    cards[i].style.display = ""; // TAMPILKAN
                    foundCount++;
                } else {
                    cards[i].style.display = "none"; // SEMBUNYIKAN
                }
            }

            // 4. Kalau gak ada satupun yang cocok, munculin pesan sedih
            const noMsg = document.getElementById('noResultMsg');
            if (foundCount === 0) {
                noMsg.style.display = "block";
            } else {
                noMsg.style.display = "none";
            }
        }
    </script>
</body>
</html>